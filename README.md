## Project Setup

### Backend
cd backend
npm install
node server.js

### Frontend
cd frontend
npm install
npm run dev


This project uses a monorepo structure for simplicity and easier evaluation.
In production, services can be split into independent repositories.